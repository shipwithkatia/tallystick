OpenAI reader against the native AgentHallu reader
================================================================
trajectories read      693  (2 file(s) skipped: not a trajectory)
identical              582  (84%)
different              111

where they differ:
    86  root/derived differs on 1 artifact(s)
    13  root/derived differs on 2 artifact(s)
     8  root/derived differs on 3 artifact(s)
     3  root/derived differs on 5 artifact(s)
     1  root/derived differs on 6 artifact(s)

artifact kinds, native reader vs OpenAI reader:
  document          693     693
  final_answer      693     693
  intermediate     3063    3220
  tool_result      3055    2898

first disagreements:
  BFCL/000.json: 20 vs 20 artifacts
  BFCL/001.json: 14 vs 14 artifacts
  BFCL/002.json: 25 vs 25 artifacts
  BFCL/003.json: 9 vs 9 artifacts
  BFCL/005.json: 9 vs 9 artifacts
  BFCL/006.json: 11 vs 11 artifacts
  BFCL/007.json: 9 vs 9 artifacts
  BFCL/009.json: 9 vs 9 artifacts
  BFCL/010.json: 8 vs 8 artifacts
  BFCL/011.json: 14 vs 14 artifacts

with every tool_call_id and tool name stripped:
  identical              590  (85%)
  artifacts read as the wrong kind  148
  With neither, a result can only be matched to its call by the
  order the calls were declared in. Three of the laundering
  defects found in review of this release lived on that matching
  code - and this corpus can decide almost nothing about it:
       204  parallel tool batches
       118  of them calling more than one distinct tool
         1  of those mixing a tool that hands back the model's
            own text with one that does not - the only shape where
            a mis-assignment can move an artifact across the root
            line and change a verdict
  So this pass is a regression guard, not evidence: the coverage for
  that code is in tests/test_convert.py, written from the failing
  inputs review found, because the corpus does not contain them.

with no --tool-returns-model-text flags at all:
  identical              420  (61%)
  The headline above names this corpus's four echo tools. Without
  them a final_answer tool's output is a root, and the two readers
  part on a third of the corpus. That is not a defect in either:
  it is the size of what only the operator knows, and the reason
  the flag exists.

if the interpreter were named --tool-returns-model-text:
  identical              548  (79%)
  roots turned into model text  338, of which:
       169  no search and no final_answer - plausibly computed
       138  code that called a web tool - fetched text, not computed
        24  code calling final_answer - an echo the native reader missed
         7  an interpreter error message
  -> worse, and not for the obvious reason: most of what the flag
     would demote is web text surfaced through the interpreter, not
     a computed value - and a few are echoes the native reader's own
     8-character guard let through. The flag is for a tool that
     ALWAYS hands back the model's words. An interpreter does not.

what this comparison cannot see:
  34 interpreter output(s) in this corpus meet every condition of
  the native reader's echo test except its 8-character minimum. Both
  readers call them roots, so they agree, and they are counted above
  as agreement. Agreement is not correctness.
